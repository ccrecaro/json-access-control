/* import { RequestCtx } from "../../components/objects/architecture/context/RequestCtx";
import { EvaluationResult } from "../../components/objects/result/EvaluationResult";
import { DAYTIME_DURATION_EQUAL } from "../../constants/Functions";
import { argumentsEqual } from "./EqualityFunctions";

export function functionEvaluator(functionId: string, inputs: valueType, request: RequestCtx): EvaluationResult {
    var result: valueType;
    switch (functionId) {
        case DAYTIME_DURATION_EQUAL:
            result = argumentsEqual(inputs, request);
            break;
    
        default:
            break;
    }
} */